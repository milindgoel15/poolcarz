import { RideFilterPipe } from './ride-filter.pipe';

describe('RideFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RideFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
